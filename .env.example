# Spotify is optional. Signal falls back to the local System Audio engine
# without it. The client id is a public identifier — PKCE means no secret
# is ever needed in the browser, and none belongs in this file.
VITE_SPOTIFY_CLIENT_ID=
